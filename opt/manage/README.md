# Manage Modules Template

Direktori ini adalah template source untuk modularisasi `manage.sh`.

- Source di repo: `opt/manage/...`
- Target deploy di VPS: `/opt/manage/...`

`run.sh` dan `setup.sh` akan melakukan sinkronisasi best-effort ke `/opt/manage`.
