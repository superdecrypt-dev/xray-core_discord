import type { MenuDefinition } from "./types";

export const menu9: MenuDefinition = {
  id: "9",
  label: "Install BOT Discord",
  description: "Info lifecycle installer bot discord.",
  actions: [{ id: "info", label: "Install Info", mode: "direct", style: "primary" }],
};
